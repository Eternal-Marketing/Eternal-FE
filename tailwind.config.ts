import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#184BBA",
        bg: "#FFFFFF",

        main: "#111111",
        inverse: "#FFFFFF",

        sub1: "#505050",
        sub2: "#767676",
        sub3: "#999999",
      },
    },
  },
};

export default config;
