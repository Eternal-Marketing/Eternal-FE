import ColumnPageContent from './ColumnPageContent';

/** 칼럼 목록 - 바이럴 마케팅(첫 번째) 페이지, 내용은 카테고리 페이지와 동일 */
export default function ColumnPage() {
  return <ColumnPageContent activeCategoryIndex={0} />;
}
